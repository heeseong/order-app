# 이 파일을 .env로 복사하여 사용하세요
# cp env.example.txt .env

# 서버 포트
PORT=3000

# Node 환경
NODE_ENV=development

# PostgreSQL 데이터베이스 설정
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coffee_order_db
DB_USER=postgres
DB_PASSWORD=your_password

# 데이터베이스 연결 문자열 (선택사항)
# DATABASE_URL=postgresql://user:password@localhost:5432/coffee_order_db

